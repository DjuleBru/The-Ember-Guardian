using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CreatureAttack_Colossus : CreatureAttack
{
    //public override Vector3 GetAttackDir() {
    //    if ((attackTargetIDamageable as MonoBehaviour) == null) {
    //        return Vector3.zero;
    //    }

    //    if (attackTargetIDamageable is Player) {

    //        float dirToPlayer = (attackTargetIDamageable as MonoBehaviour).transform.position.x - transform.position.x;
    //        Debug.Log("dirToPlayer " + dirToPlayer);
    //        if(Mathf.Abs(dirToPlayer) < 3) {
    //            return transform.position - (attackTargetIDamageable as MonoBehaviour).transform.position;
    //        }
    //    }

    //    return (attackTargetIDamageable as MonoBehaviour).transform.position - transform.position;
    //}
}
